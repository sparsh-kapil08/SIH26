# Legal Metrology Compliance Checker — Frontend
Static HTML/CSS/JS application for Department of Consumer Affairs (SIH26034).
Deploy directly to Vercel as a Static Site.

For the separate backend deployment, set the frontend Vercel project's
`VITE_BACKEND_URL` environment variable to the backend project's full URL,
for example `https://your-backend-project.vercel.app`.
